<div class="card" data-rawg="{{rawgId}}">
    <img src="{{screenshot}}" alt="{{name}} Screenshot">
    <h2>{{name}}</h2>
    <p>Genre: {{genre}}</p>
   <p> Release Date: {{released}}</p>
   <button id="likeButton">Like</button>
</div>